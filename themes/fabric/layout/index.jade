extends includes/layout

block content
  include mixins
  if page.posts.length > 0
    ul.post-list
      each post in page.posts.toArray()
        +postItem(post, 'post').post-list__item
  include includes/pagination
